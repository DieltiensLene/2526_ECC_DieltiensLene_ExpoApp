// Force the app to use the light color scheme so the background stays white.
// If you want to respect the system setting again, revert this to re-export
// `useColorScheme` from 'react-native'.
export function useColorScheme(): 'light' {
	return 'light';
}
